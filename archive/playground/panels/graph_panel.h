#pragma once

// Graph editor panel (future)
// Will contain node spawning, connection editing, and search.
// For now, this is a placeholder.

class GraphPanel {
public:
    GraphPanel() = default;
    // void Draw(...);
};
