#pragma once

// Inspector panel (future)
// Will display the selected node's parameters, state, and bypass flag.
// For now, this is a placeholder.

class InspectorPanel {
public:
    InspectorPanel() = default;
    // void Draw(...);
};
